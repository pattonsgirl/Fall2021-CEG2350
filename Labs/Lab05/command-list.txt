echo "CPU info"
lscpu | grep "Model name:"
echo "System memory"
free -h --total | grep "Total:"
echo "Disk space"
df -h | egrep "[xs][v?]d*"
echo "Disk partition info"
# needs sudo
sudo fdisk -l /dev/[xs][v?]d*
echo "Looking for GPU"
# needs sudo
lshw -class display
